# Week 4 product design

`week4-dashboard-concept.png` is the visual reference generated for the recommendation workspace. The implementation keeps its evidence-first three-column information architecture while sourcing every score, date, status, and excerpt from the application API.

The reference is intentionally not a data fixture and must never be used as one.
